<%= form_for(@micropost) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="field">
		<%= f.text_area :content, placeholder: "Напишите новый пост..." %>
	</div>
	<%= f.submit "Пост", class: "btn btn-large  btn-primary"%>
<% end %>